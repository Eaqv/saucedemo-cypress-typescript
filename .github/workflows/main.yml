name: E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  e2e-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20 # Versión estable
          cache: 'npm'    # Cache para velocidad

      - name: Install dependencies
        run: npm ci      # 'ci' es más rápido y limpio que 'install'

      - name: Run Cypress tests
        run: npm run cy:run

      - name: Upload Test Artifacts
        uses: actions/upload-artifact@v4
        if: always() # Se suben aunque fallen los tests
        with:
          name: cypress-artifacts
          path: |
            cypress/screenshots
            cypress/videos
          retention-days: 5